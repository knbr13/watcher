write:
  - echo "this command will run on write events"
  - echo "this is another command that will run on write events"
rename:
  - echo "this command will run on rename events"
remove:
  - echo "this command will run on remove events"
chmod:
  - echo "this command will run on chmod events"
create:
  - echo "this command will run on create events"
common:
  - echo "this command will run on every event occurred"
